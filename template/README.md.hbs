# {{name}}

This project was bootstrapped with [Create OpenAPI Client](https://github.com/developertown/create-openapi-client).

## Available Scripts

### `npm generate`

Downloads an updated version of the openapi.json file.<br />
Outputs code generated by the [openapi generator](https://github.com/OpenAPITools/openapi-generator-cli) using the [typescript-fetch](https://github.com/OpenAPITools/openapi-generator/blob/master/docs/generators/typescript-fetch.md) generator

### `npm build`

Builds the package for production to the `dist` folder.<br />
Your package is ready to be installed!

## Installation

```
$ yarn add {{name}}
```

## Usage

```ts
import { SomeApi } from "{{name}}";

const api = new SomeApi();

const result = await api.someGeneratedApiMethod();
```
